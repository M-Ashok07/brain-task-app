apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brain-task-chart.fullname" . }}-test-connection"
  labels:
    {{- include "brain-task-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "brain-task-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

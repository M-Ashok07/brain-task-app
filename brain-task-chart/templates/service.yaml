apiVersion: v1
kind: Service
metadata:
  name: {{ include "brain-task-chart.fullname" . }}
  labels:
    {{- include "brain-task-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "brain-task-chart.selectorLabels" . | nindent 4 }}
